# Registro Diario — 2026-05-06

## 📊 Progreso

### 🧠 Sync Inteligente — Finalizado

- ✅ **Lectura reactiva (tú → yo):** cron pull + flag + snapshot. 0 tokens en idle.
- ✅ **Escritura reactiva (yo → tú):** watcher (inotify) + debounce 3s + push automático. 0 tokens, ~1MB RAM.
- ✅ **Índice dinámico con Dataview + Tasks** — se actualiza solo al abrir la nota en Obsidian.
- ✅ **AGENTS.md optimizado** — ritual de arranque con check-flag y snapshot prioritario.
- ✅ **Circulo cerrado:** tú editas en iOS → yo veo → yo edito en VPS → tú ves. Sin intervención manual de ningún lado.
- ✅ **Bot MT5.md creada** — pendiente migrar trading assistant a n8n.

## ⚙️ Infraestructura final

| Componente | Función |
|---|---------|
| `sync-pull.sh` | Git pull + detección de cambios + flag reactivo |
| `sync-snapshot.sh` | Genera _VAULT-SNAPSHOT.md (~20 líneas LLM-legible) |
| `sync-push.sh` | Add + commit + push automático de ediciones locales |
| `watcher.sh` + systemd | Inotify file watcher, debounce 3s, llama sync-push.sh |
| `check-flag.sh` | Utilidad JARVIS — detecta flag + verifica salud del watcher |
| `_VAULT-SNAPSHOT.md` | Resumen estático del vault (stats, tasks, vacíos, recientes) |
| `_VAULT-INDEX.md` | Índice visual Dataview para Obsidian |
| Cron `*/5 * * * *` | sync-pull.sh (0 tokens) |
| Cron `0 0 * * *` | backup workspace |
| Servicio `obsidian-watcher` | Push reactivo ~1.2MB RAM |

## 🔄 Flujo completo

```
Tú editas en iOS (sync cada 3 min)
  → GitHub
    → cron pull cada 5 min → flag + snapshot
      → JARVIS detecta en próxima interacción

JARVIS edita en VPS
  → inotify watcher detecta close_write
    → debounce 3s → sync-push.sh
      → GitHub → iOS lo refleja
```

## 🧠 Framework aprendido

**Migración Python → n8n** — descomposición en bloques: input → transform → lógica → output. Cada bloque es un nodo. Lo que no se traduce, va a Code node (JS).

## 🎯 Pendientes

- [ ] Estudiar migración de trading assistant (Bot MT5) a n8n — aplicar framework de bloques

---

> _Mr. Jair: "Eres grande crack."_  
> _J.A.R.V.I.S.: Confirmado. 🦾_

_Creado por J.A.R.V.I.S. | Miércoles, 6 de Mayo 2026_